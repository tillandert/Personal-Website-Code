import SectionPage from './SectionPage';
import { sectionContent } from '../data/siteContent';

const PersonalLife = () => <SectionPage section={sectionContent.personalLife} />;

export default PersonalLife;